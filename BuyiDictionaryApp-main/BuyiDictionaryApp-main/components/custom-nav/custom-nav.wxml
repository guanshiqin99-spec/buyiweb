<view class="custom-nav" style="--status-bar-height: {{statusBarHeight}}px; --nav-text-color: {{textColor}};">
  <view class="back-btn" wx:if="{{showBack}}" bindtap="onBack">
    <view class="back-arrow"></view>
  </view>
  <text class="nav-title" wx:if="{{title}}">{{title}}</text>
</view>
