# 待修改问题清单

> 记录时间：2026-07-22
> 最后更新：2026-08-02（核实 2-5 项待办项的实际实现状态，更新完成情况标注）
> 说明：本文档仅记录待修改问题，暂不进行代码修改。

## 一、小程序端待修改问题

### 1. ✅ 小程序端新增 AI 功能（已完成）
- 状态：已于 2026-07-23 完成
- 已接入内容：
  - 新增 `components/agent-panel/`（AI 导览员悬浮入口）+ `utils/agentStream.js`
  - 已在 `app.json` 全局注册 `agent-panel`，并挂载到 `pages/home`、`pages/query`、`pages/quiz` 三个页面
  - `cloudfunctions/apiProxy/index.js` 增加 SSE 流式累积逻辑（`requestStream`），对 `/miniapp/agent/*` 路径累积完整响应后一次性返回；`cloudbaserc.json` 云函数超时调整为 60s，`app.js` 的 `wx.cloud.callFunction` 透传 `timeout`
  - query 页新增「AI 造句 / AI 推荐」工具区，quiz 页新增「AI 五题挑战」入口
- 与 Web 端 `AgentPanel` 对齐，通过云函数 `apiProxy` 对接后端 `/miniapp/agent/*`

### 2. 🟡 歌曲封面与轮播图（部分完成）
- 状态：轮播图与封面机制已实现，差本地 covers 目录
- 已实现：
  - 首页轮播图：`pages/home/index.wxml:82-96` 配置 `swiper` 组件，`pages/home/index.js:42-63` 调用 `homeApi.get()` 拉取 banner，失败回退 `/assets/images/banner1.jpg`
  - 歌曲封面：`pages/song/index.wxml:30,59` 优先用后端返回的 `item.image`，无则回退本地 `banner1.jpg`
- 待办：小程序本地 `assets/images/covers` 目录缺失，封面图完全依赖后端 `backend/uploads/covers/` 动态返回。若后端不可达，封面将全部回退为 banner1.jpg

### 3. ✅ 小程序歌曲列表样式优化（已完成）
- 状态：已于 2026-08-02 核实完成
- 已实现方案：
  - `pages/song/index.wxml:56-77` 横向"唱片卡"布局：左封面缩略图（140×100rpx）+ 中信息列 + 右收藏按钮
  - 信息层级精简为：标题（30rpx 加粗）→ 副信息演唱者（22rpx）→ meta 类型+时长（20rpx 浅色）
  - 已移除英文与冗余长描述，所有文案均为中文短文本并做 fallback 处理
  - 样式定义见 `pages/song/index.wxss:202-286`

### 4. ✅ 小程序微信登录（已完成）
- 状态：已于 2026-08-02 核实完成，全链路已闭环
- 已实现流程：
  - `pages/login/login.js:85-100` 调用 `wx.login` 获取 code
  - `utils/api.js:211-221` 调用后端 `/miniapp/auth/wechat-login`（经云函数 `apiProxy` 透传）
  - `app.js:261-299` 写入 `globalData` 与 `wx.setStorageSync('loginState', ...)` 持久化
  - `app.js:41-44` 启动时从 storage 恢复登录态
  - `utils/api.js:158-169` + `app.js:301-340` 401 自动刷新 token
  - `app.js:342-366` 登出调用 `/miniapp/auth/logout` 并清理登录态
  - `pages/login/login.js:67-82` 头像上传至云存储

### 5. ❌ 文案排版：「从一个词，走进一座文化馆」（未完成）
- 状态：文案在代码库中不存在，需先确认是否还要使用该文案
- 核实结果：全库搜索「从一个词」「走进一座」均 0 匹配；`white-space: nowrap` 共 10 处但无一与该文案相关
- 待办：若保留该文案，需先在首页/引导页添加文案，再对其做防断行处理（`white-space: nowrap` 或 `<text>内嵌<text>` 包裹"文化馆"）

### 6. ✅ 小程序 AI 导览员弹窗遮挡导航栏（已完成）
- 状态：已于 2026-07-23 完成
- 原诉求：小程序中「布依文化导览员」AI 面板展开时会覆盖底部 TabBar 导航栏，导致无法切换页面
- 已实现方案：
  - `components/agent-panel/agent-panel.js` 新增 `_toggleTabBar` 方法：面板展开时 `wx.hideTabBar`，关闭时 `wx.showTabBar`；同时通过 `app.eventBus` 广播 `tabbar:hide` / `tabbar:show` 事件，并在组件 `detached` 时恢复 TabBar
  - `components/agent-panel/agent-panel.wxss` 调整输入区 `padding-bottom` 为 `calc(24rpx + env(safe-area-inset-bottom))`，适配全面屏安全区
- 涉及页面：`pages/home`、`pages/query`、`pages/quiz`（已全局注册 `agent-panel`）

### 7. ✅ web词典界面，竖屏条件下词语详解卡片会到最底下（已完成）
- 状态：已于 2026-07-23 完成
- 原诉求：竖屏条件下数据量大时需一直下滑才能看到详解卡片，考虑竖屏时点击词语弹出弹窗
- 已实现方案：
  - 抽出 `buyi-dictionary-vue/src/components/specific/DictionaryEntryDetail.vue` 复用详情渲染逻辑
  - `Dictionary.vue` 在 `@media (max-width: 860px)` 下隐藏右侧 sticky 详情列，改为点击词条弹出底部悬浮弹窗（`.mobile-detail-modal`）
  - 弹窗支持遮罩点击关闭、ESC 关闭、底部上滑面板样式、`Transition` 过渡动画，最大高度 78vh 内部滚动

## 二、图片资源说明

### 1. 缺图处理策略
- 缺失的图片资源可使用 AI 生成图片补充
- 生成时需符合布依族文化主题视觉风格

### 2. 已恢复的小程序封面图资源
- 路径：`D:\BuyiDictionaryWeb\BuyiDictionaryApp-main\BuyiDictionaryApp-main\assets\images\covers`
- 当前包含的封面文件：
  - `cover_fenshoudiao.jpg` / `cover_fenshoudiao_new.jpg` / `cover_fenshoudiao_old.jpg`
  - `cover_huiyouge.jpg` / `cover_huiyouge_old.jpg` / `cover_huiyouge_v2.jpg` / `cover_huiyouge_v3.jpg`
  - `cover_yushuiqing.jpg` / `cover_yushuiqing_new.jpg` / `cover_yushuiqing_old.jpg`
- 后续再决定是否删除其中冗余的旧版本图片

所有内容有把握不好的请直接问我
