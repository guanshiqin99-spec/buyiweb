# 冗余文件清理清单

> 生成时间：2026-07-23，最后更新：2026-07-23。基于全库引用扫描（grep 无引用 + git ls-files 追踪状态核对）。
> **执行原则**：先移动到根目录 `_trash_review/`（保留目录结构），本地跑通 `启动竞赛包.bat` 全流程 + `npm run build` 后再真正删除。已被 git 追踪的项需要 `git rm -r --cached`（保留工作区副本到 _trash_review）后提交。
> ⚠️ 删除文件属于用户红线操作，执行前必须获得用户明确确认。

## 已完成的附带修复（D 类）

- ✅ **D1 已完成（2026-07-23）**：`启动前端.bat` / `启动后端.bat` / `导入示例数据.bat` 三个脚本中的硬编码 `D:\BuyiDictionaryWeb\...` 已改为 `%~dp0` 相对路径，仓库迁移到任意目录均可直接运行。
- ⏳ **D2 待处理**：`BuyiDictionaryApp-main/BuyiDictionaryApp-main/backend/.gitignore` 仍需补 `dist/`，防止 A8（构建产物入库）复发。

## 已完成的额外清理（清单外，2026-07-23）

- ✅ 删除根目录 `DEPLOY.md`（旧版部署文档，已被重写后的 `部署方案.md` 取代）
- ✅ 删除 `BuyiDictionaryApp-main/BuyiDictionaryApp-main/backend/vercel.json`（Vercel 弱 JWT_SECRET 漏洞文件，见 02-清单 V3），并通过 Vercel CLI 下线并删除 `backend` / `buyi-dictionary-vue` 两个 Vercel 项目

---

## A. 可安全删除（代码零引用，已逐一核实）

| # | 路径 | 大小 | git 追踪 | 性质与依据 |
|---|------|------|----------|-----------|
| A1 | `temp_extract/` | 162 MB | 否 | 压缩包解压残留（民歌演示音频/词汇录音演示），全库无代码引用 |
| A2 | `布依词典-竞赛提交.zip` | 12 MB | 否（.gitignore 排除 *.zip） | 早期提交包（22 个文件：assets/audio/images/index.html），是旧版单页静态站，与现 Vue 站无关 |
| A3 | `buyi-dictionary-vue/.git_backup/` | 51 MB | **是（数千文件）** | `.git` 目录的手工备份被误提交，纯垃圾 |
| A4 | `BuyiDictionaryApp-main/BuyiDictionaryApp-main/.git_backup/` | 19 MB | **是** | 同上（两处 .git_backup 合计约 6998 个被追踪文件，占整库 9380 个追踪文件的 74%） |
| A5 | `original_assets_backup/` | 46 MB | 部分 | 资源备份目录，.gitignore 已排除但早期部分文件已入库；代码无引用 |
| A6 | `design_concepts/ui-audit-2026-07-14/profile-04-culture-desktop/`、`profile-05-dictionary-mobile/` | 32 MB | **是（712 文件）** | **Edge 浏览器 profile 数据（含 Crashpad 崩溃转储等）被误提交，有隐私风险**，与"UI 审计截图"无关；同目录 5 张 `.png` 截图可保留 |
| A7 | 根 `buyi_audio/` | 12 MB | 是 | 与 `buyi-dictionary-vue/public/audio/` **文件名完全一致的重复副本**；前端实际使用的是 public/audio；此目录只被旧部署文档的 scp 命令引用 |
| A8 | `backend/dist/`（BuyiDictionaryApp 内） | — | **是（398 文件）** | 构建产物入库；`npm run build` 随时可重建。从 git 移除即可，本地文件可留 |
| A9 | `BuyiDictionaryApp-main/BuyiDictionaryApp-main/buyidict-api/` | 小 | 是 | 腾讯 CloudBase"函数型云托管"**官方示例模板**（echo/sse/ws demo），与本项目后端无关 |
| A10 | 一次性调试脚本：`check_home.py`、`check_published.py`、`check_remote_db.py`、`check_remote_db2.py`、`compare_db.py`、`full_api_test.py`、`test_api.py`、`test_dict.py`、`test_login_detail.py`、`verify_data.py` | 小 | 是 | 针对旧阿里云环境的一次性排查脚本 |
| A11 | `upload_frontend.py` | 小 | **是** | ⚠️ **第 7 行含明文 SSH 密码，必删**（见 02-漏洞清单 V1，删除后还需改服务器密码） |
| A12 | `cursor-glow-demo.html`、`布依词典-创意方案.html`、`test_exec_result.png`、`backup_vue_views_before_rollback.patch` | ~300 KB | 是 | 演示 demo / 早期创意稿 / 测试截图 / 回滚备份补丁，均无引用 |
| A13 | 根 `package-lock.json` | 102 B | 是 | 根目录没有 package.json，这是个空壳锁文件 |
| A14 | `buyi-dictionary-vue/functions/api/health.js` | 1 KB | 是 | 硬编码旧 IP 的健康检查特例，P0-0 重写代理后由 `[[path]].js` 覆盖 |
| A15 | 前端未引用资产（见下方明细） | ~几 MB | 是 | grep 全 src 零引用 |

A15 明细（`buyi-dictionary-vue/src/` 内，均 0 引用）：
- 图片：`assets/images/bg-dictionary-new.jpg`、`bg-phrases.jpg`、`bg-profile.png`、`bg-search.png`、`bg-vocabulary.jpg`、`bouyei-textile.jpg`
- 图标组件：`components/icons/IconCheck.vue`、`IconChevronLeft.vue`、`IconCulture.vue`、`IconHome.vue`、`IconMinus.vue`、`IconMusic.vue`、`IconPlus.vue`、`IconVolumeMute.vue`
- ⚠️ 保留 `IconBase.vue`（被 20 个文件引用）及其余所有图标/图片

## B. 需用户确认后再决定

| # | 路径 | 大小 | 说明 |
|---|------|------|------|
| B1 | `buyi-dictionary-web-draft/` | 17 MB | 早期 HTML 原型，README 标注"归档"。若竞赛材料需展示设计演进过程则保留，否则可移出仓库另存 |
| B2 | `deploy_ssh.py`、`upload_backend.py`、`upload_db.py`、`setup_nginx.py` | 小 | 已环境变量化（无明文密码），是阿里云 ECS 部署工具。**若最终部署走 Sealos/Cloudflare 则全部可删**；若仍可能回到 ECS 方案则保留 |
| B3 | `docker-compose.yml`（根）vs `deploy/docker-compose.yml` vs `sealos-deploy.yml` | 小 | 三套部署配置并存。确定最终部署路径后删掉不用的两套，避免误用（注意根 docker-compose 与 deploy/ 内那份不完全相同） |
| B4 | `buyi_dictionary.db`（根，90 KB） | 90 KB | 被 `启动后端.bat` 用作本地演示种子库（复制为 buyi-local.sqlite）。**建议保留**，除非确认后端 `seed:data` 可完全替代 |
| B5 | `design_concepts/ui-audit-2026-07-14/*.png`（5 张截图） | 3.5 MB | UI 审计截图，可留作过程材料 |
| B6 | `buyi-dictionary-vue/docs/evidence/`（9 张截图，6.8 MB）及 `docs/*.md` 溯源文档 | 6.8 MB | 素材溯源/对比度取证，**建议保留**（社会价值维度评审可能用到） |
| B7 | `skills/`、`skills-lock.json`、`.trae/` | 小 | AI 开发工具（Trae/CloudBase skill）配置，不影响运行；保留无害 |
| B8 | `BuyiDictionaryApp-main/BuyiDictionaryApp-main/` 双层嵌套 | — | 解压残留导致的同名双层目录。**赛前不要动**——bat 脚本、文档、部署脚本大量引用双层路径，扁平化收益低、风险高 |

## C. 必须保留（曾被怀疑但确认在用）

- `buyi-dictionary-vue/public/audio/`（12 MB）——民歌播放的本地回退音源，`src/data/playableSongs.js` 引用
- `backend/uploads/buyi_audio/`——后端媒体文件
- `buyi-dictionary-vue/tests/` 全部——`npm run test` 显式引用
- `启动竞赛包.bat`——唯一路径正确的启动脚本（相对路径）
- `backend/buyi-local.sqlite`——本地开发数据库
- 根目录 `ARCHITECTURE.md`/`README.md` 等文档——参赛材料

## D. 附带修复（清理时顺手做，各一行改动）

1. ✅ **已完成（2026-07-23）**：`启动前端.bat:10`、`启动后端.bat:10-11`、`导入示例数据.bat:10` 中硬编码的 `D:\BuyiDictionaryWeb\...` 已改为 `%~dp0` 相对路径（参照 `启动竞赛包.bat` 的写法），仓库迁移目录后不再报错
2. ⏳ **待处理**：在 `BuyiDictionaryApp-main/BuyiDictionaryApp-main/backend/.gitignore`（现仅有 `.vercel`）补 `dist/`，防止 A8 复发

## 执行顺序建议

```
1. git 检查工作区干净 → 新建分支 cleanup/pre-final
2. 处理 A11（含密码文件，最优先）
3. mkdir _trash_review，把 A 类逐项 move 进去；git rm -r --cached 处理已追踪项
4. 修 D1/D2
5. 验证：启动竞赛包.bat 全流程可用；cd buyi-dictionary-vue && npm run build && npm run test 通过
6. 提交。B 类逐项询问用户后处理
7. 观察 2-3 天无异常后删除 _trash_review（再次询问用户）
```
